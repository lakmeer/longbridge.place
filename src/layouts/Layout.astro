---
import '../base.css'
import { ViewTransitions } from 'astro:transitions'

import Title from '@/components/Title.astro'

interface Props {
  title: string;
  cl?:   string;
}

const { title, class: cl } = Astro.props
---
<!doctype html>
<html lang="en" class="bg-sky-day" style="background-attachment:fixed">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Astro description" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <ViewTransitions />
  </head>
  <body class:list={[ cl, "text-white min-h-screen" ]}>
    <main class="py-[5vw] px-8 xl:px-16 max-w-7xl mx-auto">
      <Title />

      <div class="mx-auto px-8 grid mt-[5vw] pt-8">
        <slot />
      </div>
    </main>
  </body>
</html>
