---
interface Props {
  article: any;
  title: string;
}

const { article, title } = Astro.props;
const { Content } = await article.render();
---

<div class="prose mx-auto">
  <h2 class="font-title-lesser mb-4">{article.title}</h2>
  <Content />
</div>
